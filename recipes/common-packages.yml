modules:
  - type: dnf
    install:
      packages:
        - btop
        - f39-backgrounds-kde
        - ibm-plex-fonts-all
        - mullvad-vpn
        - neovim
        - rclone
        - rsms-inter-fonts
    remove:
      packages:
        - NetworkManager-config-connectivity-fedora