modules:
  - type: dnf
    install:
      packages:
        - btop
        - f39-backgrounds-kde
        - mullvad-vpn
        - nvim
        - rclone
    remove:
      packages:
        - NetworkManager-config-connectivity-fedora